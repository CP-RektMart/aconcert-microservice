apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-service
  namespace: aconcert
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-service
  template:
    metadata:
      labels:
        app: event-service
    spec:
      containers:
        - name: event-service
          image: boom9387/event-service:latest
          ports:
            - containerPort: 8002
          envFrom:
            - secretRef:
                name: event-env
---
apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: aconcert
spec:
  selector:
    app: event-service
  ports:
    - port: 8002
      targetPort: 8002
  type: ClusterIP
